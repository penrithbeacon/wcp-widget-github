services:
  github:
    build: .
    image: wcp-widget-github
    container_name: wcp-widget-github
    ports:
      - "3743:3743"
    volumes:
      - gh-data:/app/data
      - ./published:/app/published
    restart: unless-stopped

volumes:
  gh-data:
